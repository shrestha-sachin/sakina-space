# Notion Integration — Environment Variables
# Copy this file to .env and fill in your values.

# 1. Create a Notion integration at https://www.notion.so/my-integrations
# 2. Copy the "Internal Integration Secret" here:
NOTION_API_KEY=

# 3. Create two Notion databases and share them with your integration
# 4. Copy each database ID from the URL (the 32-char hex string before the ?v=)
NOTION_CLIENT_DB_ID=
NOTION_THERAPIST_DB_ID=

# Client database columns needed:
#   Name (Title), Email (Email), Language (Select), Cultural Background (Text), Support Type (Select), Submitted At (Date)
#
# Therapist database columns needed:
#   Name (Title), Email (Email), License Type (Select), States Licensed (Text), Cultural Alignment (Text), Submitted At (Date)
